query GetProductById(
  $id: String!
) {
  product(id: $id){
    name
    gallery
    description
    attributes {
      id
      name
      items {
        displayValue
      }
    }
    prices {
      currency {
        label
        symbol
      }
      amount
    }
    brand
  }
}
