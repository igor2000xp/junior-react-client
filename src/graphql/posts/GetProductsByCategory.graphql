query GetProductsByCategory(
  $category: String = "all"
) {
  category(
    input: {
      title:
      $category
    }
  ) {
    products {
      id
      name
      inStock
      gallery
      brand
      prices {
        currency {
          label
          symbol
        }
        amount
      }
    }
  }
}
