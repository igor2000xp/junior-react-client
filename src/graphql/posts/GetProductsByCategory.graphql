query GetProductsByCategory(
  $category: String = "all"
) {
  category(
    input: {
      title:
      $category
    }
  ) {
    products {
      id
      name
      gallery
      prices {
        currency {
          label
          symbol
        }
        amount
      }
    }
  }
}
